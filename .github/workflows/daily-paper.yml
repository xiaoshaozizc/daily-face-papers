name: Daily Face Papers Update

on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  update-papers:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Run script
        run: python scripts/main.py

      - name: Update README
        run: |
          python -c "
          import json
          from datetime import datetime

          today = datetime.now().strftime('%Y-%m-%d')

          # 读取论文数据
          with open('papers.json', 'r', encoding='utf-8') as f:
              papers = json.load(f)

          # 统计
          total = len(papers)
          recognition = len([p for p in papers if p.get('category') == '人脸识别'])
          generation = len([p for p in papers if p.get('category') == '人脸生成'])

          # 读取 README
          with open('README.md', 'r', encoding='utf-8') as f:
              readme = f.read()

          # 替换日期
          readme = readme.replace('今日推荐 (', f'今日推荐 ({today}').replace('今日推荐 (2025-', f'今日推荐 ({today}')

          # 替换统计数字
          readme = readme.replace('人脸识别论文**: ' + str(recognition) + ' 篇', '人脸识别论文**: ' + str(recognition) + ' 篇')
          readme = readme.replace('人脸生成论文**: ' + str(generation) + ' 篇', '人脸生成论文**: ' + str(generation) + ' 篇')
          readme = readme.replace('总计**: ' + str(total) + ' 篇', '总计**: ' + str(total) + ' 篇')

          # 如果没有匹配到数字（首次运行），使用固定格式
          import re
          readme = re.sub(r'\*\*人脸识别论文\*\*: \d+ 篇', f'**人脸识别论文**: {recognition} 篇', readme)
          readme = re.sub(r'\*\*人脸生成论文\*\*: \d+ 篇', f'**人脸生成论文**: {generation} 篇', readme)
          readme = re.sub(r'\*\*总计\*\*: \d+ 篇', f'**总计**: {total} 篇', readme)

          with open('README.md', 'w', encoding='utf-8') as f:
              f.write(readme)

          print('README updated: ' + str(total) + ' papers')
          "

      - name: Configure Git
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"

      - name: Commit and push
        run: |
          git add -A
          git diff --quiet || git commit -m "📚 Update papers - $(date +%Y-%m-%d)"
          git push
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
